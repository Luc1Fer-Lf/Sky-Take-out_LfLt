<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <!--根据条件查询购物车-->
    <select id="select" resultType="com.sky.entity.ShoppingCart">
        SELECT * FROM shopping_cart
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="dishId != null">AND dish_id = #{dishId}</if>
            <if test="setmealId != null">AND setmeal_id = #{setmealId}</if>
            <if test="dishFlavor != null">AND dish_flavor = #{dishFlavor}</if>
        </where>
    </select>

    <!--存入数据-->
    <insert id="insert">
        INSERT INTO shopping_cart (name , user_id, dish_id, setmeal_id, dish_flavor, image , number, amount, create_time)
        VALUES (#{name} , #{userId}, #{dishId}, #{setmealId}, #{dishFlavor} , #{image} , #{number}, #{amount}, #{createTime})
    </insert>

    <!--更新数量数据-->
    <update id="updateById">UPDATE shopping_cart SET number = #{number} WHERE id = #{id}</update>

    <!--查询当前用户的购物车数据-->
    <select id="list" resultType="com.sky.entity.ShoppingCart">SELECT * FROM shopping_cart WHERE user_id = #{userId}</select>


    <!--清空当前用户的购物车数据-->
    <delete id="deleteByUserId">DELETE FROM shopping_cart WHERE user_id = #{userId}</delete>

    <!--根据菜品id和用户id删除购物车数据-->
    <delete id="deleteByDishIdAndUserId">DELETE FROM shopping_cart WHERE user_id = #{userId} AND dish_id = #{dishId}</delete>

    <!--根据套餐id和用户id删除购物车数据-->
    <delete id="deleteBySetmealIdAndUserId">DELETE FROM shopping_cart WHERE user_id = #{userId} AND setmeal_id = #{setmealId}</delete>

    <!--查询当前用户的购物车数据 -订单用-->
    <select id="listByUserId" resultType="com.sky.entity.ShoppingCart">
        SELECT  name, image,  dish_id, setmeal_id, dish_flavor, number, amount
            FROM shopping_cart WHERE user_id = #{userId}
    </select>

    <!--批量插入购物车数据-->
    <insert id="insertBatch">
        INSERT INTO shopping_cart (name , user_id, dish_id, setmeal_id, dish_flavor, image , number, amount, create_time)
        VALUES
        <foreach collection="shoppingCartList" item="item" separator=",">
            (#{item.name} , #{item.userId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor} , #{item.image} , #{item.number}, #{item.amount}, #{item.createTime})
        </foreach>
    </insert>
</mapper>